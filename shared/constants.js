(function() {
  const STORAGE_NAME = 'todaily';
  const APP_NAME = 'Todaily';
  const SLOGAN = 'Effective Daily Standups';
  const DESCRIPTION = 'Avoid Wasting Time on Daily Standup Meetings';
  const EXTENSION_ELEMENT_ID = `${APP_NAME.toLocaleLowerCase()}-extension`;

  window.CONSTANTS = {
    STORAGE_NAME: STORAGE_NAME,
    APP_NAME: APP_NAME,
    SLOGAN: SLOGAN,
    DESCRIPTION: DESCRIPTION,
    EXTENSION_ELEMENT_ID: EXTENSION_ELEMENT_ID,
  };
})();
